# Eval runner facts for design 16 (2026-08)

- **DeepEval 4.x**: Python-native, pytest-style; programmatic APIs `evaluate()` (returns results), `evals_iterator()` (dataset-based), `assert_test()` (CI gating); 14+ built-in metrics incl. **agentic: task completion, tool correctness**; RAG faithfulness/relevancy; G-Eval custom judges; synthetic data generation; native tracing. Maps directly onto the evalrunner/v1 Job contract. https://deepeval.com/docs/getting-started · https://deepeval.com/docs/getting-started-agents · https://pypi.org/project/deepeval/
- **Inspect AI** (UK AISI): OSS eval framework, second runner for the slot (design 16 §8).
- Market context (positioning, not deps): OpenAI acquired Promptfoo; Braintrust $80M B — eval gating is SaaS-shaped everywhere else; k8s-native gating remains assayd's window (ADR-0006).
