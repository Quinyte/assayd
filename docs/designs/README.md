# Component design backlog

Design-first: every component gets a design doc (per `TEMPLATE.md`) approved **before** implementation. Order follows the dependency chain, not the build phases alone — contracts before consumers.

> **Scope reset, 2026-09-05 — ADR-0030.** "Critique PASS" on this table means a document survived adversarial reading. It does **not** mean the component is committed, buildable, or that any producer it names exists. The delivery commitment is now one end-to-end slice: designs **02, 03, 06, 07, 16** and the parts of **05** and **09** that slice needs. Designs **21, 23, 25, 26, 27** and the unimplemented parts of **11–14** and **18–19** are **hypotheses** — research retained, not prerequisites and not supported promises. Rows below are marked `HYPOTHESIS` accordingly. A row's PASS is evidence about the prose, and this project has now twice found a design whose central input was attributed to a producer that does not exist.

| # | Component | Phase | Size | Design status |
|---|---|---|---|---|
| 1 | KnowledgeGraphProvider contract (6-tool MCP surface + ontology schema + probe format + conformance) | P2 | M | **approved** (`01-…`, ADR-0017/0018, review PASS) |
| 2 | Agent CRD + agent-operator | P1 | L | **body consolidated 2026-09-04 (A62), critique pending — not approved** (`02-…`, ADR-0019, ADR-0029). 61 folded amendments rewritten into one self-contained body; §5 opens with every guarantee nothing enforces. CRD, revision identity by content, immutable material and the run namespace run and are e2e-proven on k3d; gateway/identity/registration/gate wait on designs 03, 06, 05, 16 |
| 3 | Policy compiler (CR → agentgateway config, incl. expose) | P1 | M | **body consolidated 2026-09-10 (PR #1); first critique REVISE (`reviews/03-consolidation-critique.md`), answered by A52; re-critique REVISE (`reviews/03-a52-recritique.md`, 4 BLOCKER), answered by A53, with B1 settled by a k3d spike (ADR-0033); third critique REVISE (`reviews/03-a53-critique.md`, 2 BLOCKER), answered by A54; fourth critique REVISE (`reviews/03-a54-critique.md`, 1 BLOCKER), answered by A55; fifth critique REVISE (`reviews/03-a55-critique.md`, 1 BLOCKER; first slice close), answered by A56, which draws the first slice (§1.1) as the only part that can be approved; sixth critique REVISE on the first slice (`reviews/03-a56-critique.md`, 0 BLOCKER, 3 MAJOR), answered by A57, with the human's J2 (ADR-0034 Amendment 2) and a measurement (`research/agentgateway-prepared-route-401-spike-2026-09.md`); seventh critique REVISE on the first slice (`reviews/03-a57-critique.md`, 0 BLOCKER, 2 MAJOR, both in `Lock`), answered by A58, with the human's decision that `Adopt` stays refused, now for consent (ADR-0034 Amendment 3); eighth critique REVISE on the first slice (`reviews/03-a58-critique.md`, 0 BLOCKER, 2 MAJOR; one step from approvable), answered by A59, which needed no human decision and records one open question for the human; ninth critique REVISE on the first slice (`reviews/03-a59-critique.md`, 0 BLOCKER, 1 MAJOR; one sentence and one test from PASS), answered by A60; tenth critique REVISE on the first slice (`reviews/03-a60-critique.md`, 0 BLOCKER, 2 MAJOR; each a sentence and a mutation from approvable), answered by A61; eleventh critique REVISE on the first slice (`reviews/03-a61-critique.md`, 0 BLOCKER, 2 MAJOR), answered by A62; twelfth critique REVISE on the first slice (`reviews/03-a62-critique.md`, 0 BLOCKER, 1 MAJOR), answered by A63; **thirteenth critique PASS on the first slice** (`reviews/03-a63-critique.md`, 0 BLOCKER, 0 MAJOR, 3 MINOR, folded in by A64); **first slice approved by the human on 2026-09-12** (ADR-0034 Amendment 4), with K2 written in by A65; A65's check REQUEST_CHANGES (`reviews/03-a65-check.md`, 0 BLOCKER, 3 MAJOR), answered by A66; A66's check APPROVE (`reviews/03-a66-check.md`, 0 BLOCKER, 0 MAJOR), minors folded in by A67; the rest of the design not approved** (`03-…`, ADR-0028 supersedes ADR-0020, ADR-0032, ADR-0034 supersedes ADR-0033). A53 writes in the human's 2026-09-11 decisions: the slice's `-auth` is **API keys** (§3.4.4), a first policy on an already-serving route is **refused** (`Adopt`), and `-auth` is one policy per Agent, because agentgateway refuses a `traffic` policy on a Service. A54 writes in three more (ADR-0033 Amendment 1): a stricter `-auth` is applied in place and trusted only after an enforcement probe (`Narrow`), `spec.budget` is refused at admission, and `expose.a2a.auth` is required with no default. A55 writes in three more, recorded with the rest as ADR-0034. A key-source change is refused while live until the administrator accepts it. A disjoint group change is make-before-break. And `-auth` follows the current spec, so a rollback never re-admits a removed group. A56 writes in three more (ADR-0034 Amendment 1). Only the first slice can be approved: one per-Agent `<agent>-auth` emitted by `Create`, with a constant key source and an admitted set that cannot change. The rest is specified, not approved. A probe that passes on the replica it reached is `Ready=True`, with an informational reason. And a served Agent whose `-auth` input becomes uncompilable keeps its last good policy. A57 writes in one more (ADR-0034 Amendment 2, J2): a served Agent moved from `auth: none` to `apikey` is locked in place by `Lock`, and trusted only after an anonymous request goes `200` → `401`, while `apikey` → `none` stays refused. A58 answers the seventh critique. `Lock`'s before-probe becomes one observation on the Agent's own card path. Every stage of `Lock` and `Create` has a deadline outcome. And a `Create` or `Lock` short of `Served` is abandoned when the desired mode changes, deleting only the policy it wrote and never recorded. A59 answers the eighth. A bare `401` proves `Lock` only while a card digest is recorded for a serving revision. An Agent whose serving route does not compile enters no transaction, so it gets no deadline, no prepared route and no page. The no-auth marker is the route label `assayd.dev/auth: "none"`. 51 amendments folded into one self-contained body; **179 cross-design claims verified first — 146 held, 32 were false** — and every false one corrected at its source (`reviews/03-consolidation-inventory.md`). §11 keeps the amendments as provenance. Four defects remain **OPEN and stated in the body**, including the canary's unsealed window. **Do not implement anything beyond the first slice**: consolidating a document does not build it. Slice PR 4 (A71) built the first slice's `Create`: a new Agent's serving `HTTPRoute` (design 07 A6.10) is published only after its emitted `<agent>-auth` refuses an anonymous request. `Adopt` is refused and recorded. A served Agent's route and policy are re-created as §3.3.3 specifies. Slice PR 5 (A72) built J2's `Lock`, K2, abandonment, a source-checked NACK driving status, and `ForeignTrafficPolicy` detection. Nothing emits an `AgentgatewayBackend`; **A75 (2026-09-13)**: the human's L1 and W1 (ADR-0034 Amendment 5). A policy on the assayd Gateway that could answer the probe holds every `-auth` transaction, and a served Agent beside any counting Gateway-level policy but authentication alone reads `GatewayAuthPolicy`; the merge and `Override` are measured on 1.5.0; **A76 (2026-09-14)**, uncritiqued: design 07 A6.15's tool-route writers, recorded where §3.2 and §6 name the route reservation; **A77 (2026-09-15)**, seven critiques FAIL and **the eighth PASS** (`reviews/03-a77-critique.md`: 1 BLOCKER, 6 MAJOR, 13 MINOR; `reviews/03-a77-recritique.md`: 0 BLOCKER, 4 MAJOR, 10 MINOR; `reviews/03-a77-critique3.md`: 0 BLOCKER, 5 MAJOR, 8 MINOR; `reviews/03-a77-critique4.md`: 1 BLOCKER, 4 MAJOR, 7 MINOR; `reviews/03-a77-critique5.md`: 0 BLOCKER, 3 MAJOR, 7 MINOR; `reviews/03-a77-critique6.md`: 0 BLOCKER, 2 MAJOR, 8 MINOR; `reviews/03-a77-critique7.md`: 0 BLOCKER, 3 MAJOR, 9 MINOR; `reviews/03-a77-critique8.md`: **PASS**, 0 BLOCKER, 0 MAJOR, 8 MINOR, none blocking implementation), all answered in §11, its measurement re-run and recorded in `research/auth-lock-false-credit-2026-09.md`, and **implemented by PR #48** (ADR-0034 Amendment 6): the human decided that the `Lock` of a missing policy re-points its route at `status.activeRevision`; A77's own calls put that at `ProbingAfter`, after A75's Gateway read and immediately before the probe, and only while a card digest is recorded for that revision, and add that no `Lock` takes a `ProbingAfter` answer until the Gateway reports on the route at its current generation (`ProbingBefore`'s one observation is not gated, A78) — a false credit **that shipped in J2's and K2's `Lock`s**. It narrows a wedge it does not remove: with no digest for the active revision either, the Agent still pages, and the body carries that state and its two exits. It is the amendment design 16's Q13 (b) described, taken separately as Q13 (a) directed; that slice is merged (`7ad39bf`, PR #40) and approved, and owed a follow-up amendment, paid by design 16 A19 and recorded in its own row; design 16 is not edited from design 03. §8.1 case 18 (a)–(h) is written (`test/envtest/authgate_test.go`), with twenty-three mutation runs tabled in A77; two of A77's own claims did not hold under measurement, and two of A78's own did not either — both about a pending candidate, the second measured HARMFUL because the revert it recommended aborts the rollout that ends the state. All four are corrected by **A78** (2026-09-16), **critique PASS** (0 BLOCKER, 1 MAJOR, 6 MINOR, all folded in), which also narrows the probe gate to `ProbingAfter` in eight places; **A79** (2026-09-17), a coverage correction that changes no rule: case 18 (h) credited (c) with pinning the re-point against the `ProbingAfter` foreign check, and (c) never reaches it — a policy at `<agent>-auth` fails the intact read at the top of that arm and the stage rewinds — so **case 18 (i) is added and owed**, the one unbuilt row in a case that was fully built. Found against merged text by two independent critiques of design 16 A19 (`reviews/16-a19-critique3.md` MINOR 2, `reviews/16-a19-critique4.md`); design 16 A19's owed row claims to be that ordering's only pin, which is correct as merged and which (i) makes false, so design 16 owes one more follow-up line, recorded under A79 in design 03 §11 because design 16 is not edited from there. **A79 passed `critique-design`** (`reviews/03-a79-critique.md`: 0 BLOCKER, 0 MAJOR, 8 MINOR, all closed; its forked pass built (i) from the text alone, and the row passed on shipped code and failed under the named mutation); **A80 (2026-09-18)**, answering a defect measured on k3d on 2026-09-14 during the PR #37 walkthrough, **passed on its fifth critique** (the five rounds are below): an Agent that has reached `Served` reports `Ready=True`, phase `Ready` and `GovernanceSkipped=False` for as long as it exists after the assayd Gateway stops accepting its serving route — rename the Gateway's listener and the route reports `Accepted=False`/`NoMatchingParent` while the Agent says it is healthy and governed, and the state never heals, because the route's `sectionName` is a compile-time constant the operator re-asserts. `routeConverged` runs only inside a `Create` or a `Lock`, `reconcileServed` asks the route only whether it carries a `backendRef`, and `policyConverged` is never reached for a served policy — so the same blindness covers the **fail-open** half too: a served `<agent>-auth` that is present and digest-equal while the Gateway reports it `Attached=False` leaves the route serving unauthenticated with `GovernanceSkipped=False`. Design 03's own §11 named this on 2026-09-12 ("once `Served`, `GovernanceSkipped=False` is not re-derived") and nothing acted on it; A80 records it as measured, and marks those two provenance bullets rather than rewriting them. A80 specifies the re-derivation on the passes that already happen — **no new object, watch, read or requeue**: the route is already read from the manager's cache and the policy already read live twice per served pass, the `HTTPRoute` watch already wakes the Agent on the Gateway's status write, and `CardDriftInterval` (5 minutes) is the backstop — adds two `PolicyApplyIncomplete` reasons, `ServingRouteNotAccepted` and `AuthPolicyNotAttached` (the second a `GovernanceSkipped` reason too), narrows §3.1's clearing rule to "not reported broken at the current generation" because the literal predicate flaps on every promotion, and owes **§8.1 case 19: twelve envtest rows and a unit table, each with a compiling mutation. Nothing in A80 is built and nothing in it is run.** **Three calls were the human's**, tabled in §11 with their costs — how aggressively to re-read (A1/A2′/A2/A3, later A4), whether to cover the policy half (B1/B2/B3), and whether to withdraw traffic (C1/C2/C3) — with **(A1), (B1), (C1) recommended and, on 2026-09-21, decided**; (B2) or (C3) reverses a safety trade this slice records and would need an ADR-0034 amendment, while (A2′) and (A2) reverse nothing. No rule of §3.3, §3.3.2 or §3.3.3's transactions moves, and A75's, A77's and A78's rules stand unamended. **A80's first draft returned FAIL from `critique-design`** (`reviews/03-a80-critique.md`: 2 BLOCKER, 6 MAJOR, 9 MINOR, all closed in the revision, and none of them asked A80 to change its mind — the diagnosis, the two-reason split, the recommendation and the cost claim all survived). The blockers were the rule rather than the finding: the gate excluded the refused `Adopt`, which always carries a transaction record and which runs no stage, while three places said it was covered; and §3.1's narrowed predicate and §3.3.3 gave opposite answers for `GovernanceSkipped` under gates of different widths, the wider of which would have weakened the predicate on the `Create` and `Lock` paths where A77 deliberately keeps §3.3.2's full tuple. Both are settled, both reasons now join A75's carry set — without which the duration page never accumulates — and choice A gains a fourth option and an honest baseline: what (A1) gives up is a **readable** Gateway that has stopped writing status, which A75's read does not cover and **nothing detects for any mode**. **A80's revision was then re-critiqued and returned FAIL** (`reviews/03-a80-recritique.md`: 0 BLOCKER, 4 MAJOR, 8 MINOR, all closed, both blockers confirmed shut, and the one disputed citation adjudicated in the drafter's favour). **Three of its four majors were the residue of the first round's own fixes**, which is the lesson worth keeping. Joining `carriedReason` — the first round's fix for the page clock — is also what makes the `-auth` step withdraw the condition entering every pass, so an unknown reading would have **retracted** a true report: a promotion moves the route's generation, the Gateway's report falls behind, and the Agent flips back to `Ready=True` on a route the Gateway is still refusing — the original defect, re-entered through the fix. **An unknown reading now HOLDS a standing report**, re-asserted from stored status on `storedHold`'s precedent, and only an explicit tuple that is not broken at the current generation clears it. `AuthPolicyNotAttached` composes on `GovernanceSkipped` — which nothing orders — as A75's own code does, rather than retiring W1's reason. **Choice A was re-derived rather than adopted**: both of the first round's corrections to it were false — (A2′) does not close the miss, because "unknown" is generation-relative, and `lastTransitionTime` records when a condition last changed, not when the Gateway last reported — so the honest baseline is that **no option closes the miss**, a fourth option (A4, the Gateway's own listeners, read on the same pass — **`spec`**, which the administrator writes and which therefore still shows a renamed listener with the gateway controller dead, rather than `status`, which the same controller writes and which survives nothing) is added for the measured defect, and (A1) is recommended for a stated reason rather than by elimination. And **rule 7: no alert rule ships anywhere in this repository** and design 10 is not built, so nothing pages for either reason today — every page claim is now conditional, and what a user sees is the condition on the Agent. **A third critique returned FAIL** (`reviews/03-a80-critique3.md`: 0 BLOCKER, 2 MAJOR, 9 MINOR, all closed; both blockers confirmed shut, all four of the previous round's majors closed, and the hold rule verified implementable against `conds.unset`, `carry` and `merge`). **Its two majors were one shape: the hold closed the unknown reading and left the two states next to it.** A pass whose `-auth` step **errored** re-derives nothing, and nothing shipped restores these two reasons there — `carriedReason` is the withdraw predicate as well as the carry list — so on the transient arm a served Agent on a refused route would have read `Ready=True` again, the defect verbatim; the no-error scope now governs **raising** only. And what is held is the reason's **claim**, not the condition object, because A80's own ordering relegates both reasons to another cause's message in exactly the states where their ranks are exercised. Case 19 is **twelve** owed envtest rows and a unit table, each with a single compiling mutation. (A4) is split into its **spec** and status readings, the spec one being the only option on the table that still sees the measured defect with the gateway controller dead. **Two caveats now sit where the human meets the choices**: the policy half is **inferred, not measured** — nothing in this repository shows an accepted route beside a policy the Gateway reports `Attached=False`, and the `make conformance-cluster` case that would measure it is owed — and design 10 is **not built** — no alert rule or dashboard ships in this repository — so the ten-minute-to-page figure rests on machinery that does not exist. Whether design 10 is also unapproved is a contradiction between its own Status line (approved) and this table (awaiting), which A80 records for `audit-docs` rather than settling. **A fourth critique returned FAIL** (`reviews/03-a80-critique4.md`: 0 BLOCKER, 3 MAJOR, 10 MINOR, all closed; everything the third round settled held, and the (A4) split was verified rather than taken). **All three majors were in one paragraph — the mechanism — and all three were the shape the previous three rounds found: the property stated, one piece of the machinery left out.** The errored-pass restoration brought the condition back and not `Ready`; the mechanism named `withholdReady` and omitted `gatewayOutcome.served`, which decides `Degraded` over phase `Pending` with `Degraded` cleared — a defect this repository records having been reintroduced once already; and the claim rule named a vehicle that cannot write `GovernanceSkipped`, leaving the ordinary policy half claiming verified authentication while `-auth` was attached to nothing. The mechanism paragraph is now an explicit list of what the judgement writes and in what order, and **A80 widens one A75 mechanism** — the caller's third error arm — which reverses no decision and which two scope sentences had denied. **A fifth critique PASSED A80** (`reviews/03-a80-critique5.md`: 0 BLOCKER, 0 MAJOR, 7 MINOR, all closed). **The mechanism paragraph was found complete on the fifth reading** — the critic implemented exactly what it lists and nothing else and traced the result against §5's rows and all twelve case-19 rows — so the four-round pattern recorded in §11 is broken, and what broke it was turning prose that asserted an outcome into a list of writes in the order they happen. That is the fact a future reader of this design most needs. The seven minors were the paperwork the claim store owed: it is the **fifth** write, its two fields are now named in §3.3.1's `status.auth` schema and recorded as an API addition, they are read before the `-auth` step because `refuseAdopt` rebuilds the record every pass, and `authKept` does not compare them, so a CRD predating them prunes them silently — stated rather than covered. **The human decided all three calls on 2026-09-21, each as recommended**: **(A1)** only an explicit `False` at the object's current generation is a failure, an unknown reading holding the last report and raising nothing; **(B1)** report the policy half — `PolicyApplyIncomplete` and `GovernanceSkipped=True`, reason `AuthPolicyNotAttached` — and act on nothing, matching W1; **(C1)** never withdraw traffic. **(A4-spec) was offered and not taken**, so the judgement reads no listeners — A75's W1 does, for a different question (A81) — and the detection gap stands: a Gateway that is readable but has stopped writing status is caught by nothing here, for any mode, less the part the hold rule recovers. **No ADR-0034 amendment is added, and that is a choice** — the three answers are what the body already specified, they reverse no trade ADR-0034 records, and no statement of it becomes false; design 16's clause-1 decision of 2026-09-17 is the precedent, and (B2) or (C3) would each need one. Every option and its costs stay in §11 as the record of what was weighed. **The two caveats the human decided with stand**: the policy half is **inferred, not measured**, and the `make conformance-cluster` case that would measure it is owed; and **no alert rule ships anywhere in this repository**, so nothing pages and what the decision produces is a duration report a human or a dashboard reads, with design 10's window unbuilt. **A80's rules are implemented by A81 (2026-09-21)**: `internal/controller/authserved.go`, `status.auth.routeRefused` and `status.auth.policyUnattached`, and §8.1 case 19's rows and its unit tables (`test/envtest/authserved_test.go`, `internal/controller/authserved_unit_test.go`) — A80's twelve, and the seven its review owed. The reproduction was driven first against `main` `53cf138`: a served Agent whose route the assayd Gateway reports `Accepted=False` at the current generation reads `Ready=True`, phase `Ready`, with no `PolicyApplyIncomplete` at all. **Building it corrected two of A80's statements, neither of which moves a rule.** `refuseAdopt` rebuilds `status.auth` every pass, so the judgement's claim write and that assignment fight: each pass clears the flag and sets it again, each write re-enqueues the Agent, and a refused `Adopt` on a refused route HOT-LOOPS — `refuseAdopt` now carries the stored flags onto the record it rebuilds. And A80's one-line `authKept` remedy reaches less than it claims: `authKept` is consulted only from `persistStatus`, which a served Agent with an empty slot never calls, so on a CRD that predates the two fields that class's hold still degrades silently; it does reach a refused `Adopt`, a route re-create, a missing-policy `Lock`, an abandonment and every transaction stage. **The policy half is still not measured** — case 19 (f) writes the report agentgateway would write and measures the operator's reaction to it — and the `make conformance-cluster` case that would measure it is still owed. **A81's own independent review returned REQUEST CHANGES (1 BLOCKER, 2 MAJOR, 9 MINOR), and the BLOCKER was A81's**: a held report's message was appended to rather than rebuilt, so it grew 240 bytes a pass on the transient error arm and 475 on the non-transient one, and **at pass 133 the API server refused every status write for that Agent** — the operator's whole status frozen during the incident this judgement exists to report, and not healing. A held message is now rebuilt from the report's own text with one note appended, the markers staying as what the tests read; measured flat afterwards (945 bytes, and 713 after 200 transient passes), and reproduced independently by the review on its own fixture (`reviews/03-a81-review.md`). Both MAJORs were unpinned claims whose deletion left the suite green — A81's own `refuseAdopt` carry, and `authKept`'s claim-store comparison, the line §3.3.3 names as what stops a pruning CRD degrading the hold to fail-open — now case 19 (n) and (o), with (m) for the accumulation and (p) for the pre-step half of the twice-evaluated gate. **MINOR 5 was rule 7 in A81's own text**: several places said nothing reads the Gateway's listeners, and `capturingListeners` reads `spec.listeners` on every served pass with a mode for A75's W1 — the judgement does not use it, which is what they say now. **All nine of the review's MINORs are answered too**, the sharpest being its MINOR 4: `routeReport` matched a route's `status.parents` entry by `parentRef` alone, and Gateway API keys one by `parentRef` AND `controllerName`, so a second controller reporting `Accepted=True` at the current generation would have CLEARED a true refusal — the judgement now matches the controller too. **A81 then took a combined `review-code` + `critique-design` pass: REQUEST CHANGES and FAIL (0 BLOCKER, 3 MAJOR, 9 MINOR), on A81's own text — and it drove A80's incident end to end on k3d, listener renamed and restored, so the headline claim holds on a real cluster.** Its majors: the `controllerName` comparison was called fail-safe and is **fail-OPEN**, since it gates raising as well as clearing, so on a cluster whose agentgateway controller is renamed the route half raises nothing and A80's defect stands unreported — stated now, with the better fix recorded as owed and assayd exposing no knob; **both halves fire on one pass** in A80's own incident, which A80 assumed they could not, so all four conditions opened by announcing a security incident the same pass had refuted — the route reason now leads; and `values.yaml` and `docs/install.md` still told administrators the defect was unreported. All nine minors taken. **A81 has not been re-critiqued since, and A80 is still not approved.** **A82 (2026-09-22) measures A80's policy half**, the `make conformance-cluster` case the case §8.1 owed — which A81's review believed would have caught its own sharpest finding, and which A82 measured would NOT have, that finding being the listener-rename state every one of them refuses at its route gate: `test/conformance/slice_attach_cluster_test.go`, three cases on agentgateway 1.5.0 (`docs/research/a80-policy-half-conformance-2026-09.md`). A byte-unchanged `<agent>-auth` reported `Accepted=True`/`PartiallyValid` on a route the Gateway still accepts — reached by a labelled key `ConfigMap` holding an entry the controller rejects, which needs no edit to anything assayd wrote — answers **`401`**; a policy reported `Attached=False` on the synthetic `StatusSummary` ancestor answers **`200`** with the backend's own body — and §5's precondition does NOT exclude it, because the digest comparison is render against render and is invariant under an out-of-band edit to the stored object. So **A80's premise is satisfied, not falsified**, and the state is entered, **repaired on the first pass after the edit** and **reported on whichever pass first reads the synthetic ancestor** — before it, with it, or after it, since that ancestor is not generation-gated. §8.1 case 19 (f) pins the CO-LOCATION, that one pass both raises and repairs, with two mutations against the operator. The window is one watch round-trip: the policy watch is label-selected with no predicate, so the edit itself enqueues the Agent. Entering it needs an identity a chart install reserves to the operator. **The bound on the shapes still unproduced is a MECHANISM**: 1.5.0 writes the real Gateway ancestor for `targetRefs` that resolved and the synthetic one for those that did not — measured on a policy with two refs, which carries both at one generation with the real one reading `Attached=True` and the route enforcing — so for an `<agent>-auth`, which has exactly one ref, the only ways to break it are to remove the route — which puts a `Create` in the slot rather than firing the route half — or to edit the ref, which the first pass after the edit repairs. **Bound**: an identity that can write the policy can strip the UID label and repoint `targetRefs` in one patch, and then nothing raises and the route serves unauthenticated standing, by §3.2's name-and-label rule. Certainty about the rest needs agentgateway's source. That two-ancestor shape is also a **second rule-8 instance**: `policyReport` short-circuits on the synthetic ancestor, so it would report not-attached against a contemporaneous `Attached=True`. So the report is reachable and is not dead code, and the fail-open consequence it announces is not true of the state that reaches it. No condition, reason, order or precondition changes; §3.3.3, §5 and §8.1 stop saying the state is unmeasured. **The human's (B) is RE-OPENED and A82 does not re-take it**: A80 recorded the choice as resting on a premise — that no fixture shows an accepted route beside a policy the Gateway reports `Attached=False` — **and the case REACHED that state**, which is new input on every cost A80 weighed rather than a failed premise: the state is judged, reported and repaired, and the shape an administrator reaches without touching anything assayd wrote is fail-closed and misreported. §11 puts B back in A80's format, with a recommendation and no decision — **and the human answered on 2026-09-23: (B1) + (B4)**, carried by **A83**. (C1), withdraw nothing, stands. Nothing tried produced `Accepted=False` on a policy, and no operator runs in that suite. **And the measurement found A81's own report naming the wrong cause in that shape**: the Gateway says `Attached=True`, "Attached to all targets", while `policyUnattachedMessage` opens "the assayd Gateway reports that it does not attach the `<agent>-auth` policy" under reason `AuthPolicyNotAttached`, and the Agent goes `Ready=False`, `Degraded` with its authentication intact — rule 8, one clause over from the one A81 fixed with its `routeOK` branch. Recorded as **OWED** in §5 and A82, not fixed by it. **A83 (2026-09-23) carries the human's (B1)+(B4) and fixes the message**: `policyBrokenMessage` branches on which of `policyReport`'s four broken answers fired, so the `PartiallyValid` shape is no longer announced as one the Gateway does not attach, and a HELD report — which re-derived no clause — says so instead of borrowing one. The REASON stays `AuthPolicyNotAttached` on every clause (D5(b): no, the claim store is one boolean and carries no clause); the synthetic short-circuit is untouched (D5(c): still open); (B2)'s re-probe and (B3) were offered and not taken; D6's silent key-source outage is untouched and still owed. It is control-plane only and the human accepted that bound. **Seventeen mutations, none INVALID: twelve killed outright and FIVE that survived**, each then pinned by a new row and killed — the errored hold's policy branch, unpinned since A81; `clauseRejected`'s `!routeOK` hedge, which no fixture reached; `judged` at the held call site; `judged` at the RAISING call site, whose literal could be flipped with the whole suite green; and the JUDGED arm of the note selector that fixed the first review round's MAJOR 1, which is that same defect with the arms swapped inside its own fix. **THREE of the four came from the two independent review rounds or from re-mutating after their fixes**, which is A83's own argument for the independent pass. Those rounds also found the split's words re-asserting, in `heldNote` and in the Gateway `message` A83 first dropped, the very claims the split removed from the lead. **A82 amends an approved slice's body and has not been critiqued; A83 has — one forked self-review and TWO independent rounds** (`reviews/03-a83-review.md`: REVISE, then REQUEST CHANGES twice, 5 MAJOR and 10 findings across them, no BLOCKER, all fixed in their round). **A84 (2026-09-23) is a rule-7 correction and is NOT approved**: §5's policy row and §3.3.3's served-`<agent>-auth` bullet both said the fail-open `AuthPolicyNotAttached` half fires only "on a pass that also read the route ACCEPTED at its current generation", and no shipped code ever gated the raise on the route — `routeOK` selects the message LEAD and gates nothing, which §3.3.3's neighbouring bullet already stated in terms, so the design contradicted itself and the code matched the correct half. The same sentence is removed from the code comment introducing the branch, **the only product code A84 touches** (it also adds envtest rows); no behaviour moves. Gating the raise is recorded as NOT recommended and left to the human, since in either shape D7 weighs it would silence a first report permanently on a renamed-`controllerName` cluster, and in the blunt shape it would also let a route reading suppress a policy claim — a posture change. A84's own first draft committed the same rule-7 error twice — an unknown reading does NOT always leave the policy half leading, and case 19 (q) DOES pin the ungated raise, both measured — caught by the author's own forked `critique-design` pass, whose record was not kept, and corrected in the round; **the independent critique, `reviews/03-a84-critique.md`, returned REVISE (1 BLOCKER, 4 MAJOR, 5 MINOR)** — its BLOCKER the same shape again, an unqualified hedged lead that `clausePartlyValid` does not have — all answered: the route reading × clause matrix is two tables in §3.3.3, the open decision is §9 D7 (gate the raise on the route; recommended NO), and §8.1 owed item 7 is one envtest row with a falsifier of its own; a second forked self-critique (`reviews/03-a84-selfcritique2.md`: REVISE, 0 BLOCKER, 2 MAJOR, 5 MINOR) found a shipped rule-8 defect — a held `GovernanceSkipped` overwrites a `ForeignTrafficPolicy` asserted on the same pass — owed as §8.1 item 9 with a code fix A84 does not make; **the independent round-2 critique** (`reviews/03-a84-critique-r2.md`: REVISE, 0 BLOCKER, 2 MAJOR, 5 MINOR) found both tables right and that the route clause was never decided — absent from A80's text, first added by A81's implementation commit — so A84 RESTORES A80's trigger; D7 weighs both shapes of a gate, and §8.1 items 7, 8 and 10 are written, leaving item 9, which needs a code change; **the independent round-3 critique** (`reviews/03-a84-critique-r3.md`: REVISE, 0 BLOCKER, 2 MAJOR, 4 MINOR) found the core holding, a stale entry in the generated condition reference (rewritten), and a hedged lead naming a route reading its own condition does not carry — shipped message text, owed as §8.1 item 11, which like item 9 needs a code change; **the independent round-4 critique** (`reviews/03-a84-critique-r4.md`: REVISE, 0 BLOCKER, 1 MAJOR, 1 MINOR, a PASS once fixed) corrected D7 — either gate shape removes item 11's false sentence, only by raising nothing on that cell — and gave item 11's row a positive assertion; both done |
| 4 | Receipt tap + receipt envelope schema | P1 | M | r2 — critique **PASS** (`04-…`, `reviews/04-review.md`; awaiting user approval + ADR-0021) |
| 5 | Agent directory (OASF on JetStream KV) | P1 | S | r2 — critique **PASS** (`05-…`, `reviews/05-review.md`; awaiting user approval, ADR-0022) |
| 6 | Identity glue | P1 | M | r2 — critique **PASS** (`06-…`, `reviews/06-review.md`; awaiting user approval, ADR-0022) |
| 7 | Umbrella chart, profiles, e2e CI | P1 | M | r2 — critique **PASS** (`07-…`, `reviews/07-review.md`; awaiting user approval, ADR-0022); amendments A1–A6, **A5 (2026-09-03), A6 (2026-09-09), A6.10 (2026-09-10), A6.11, A6.12 and A6.13 (2026-09-11), A6.14 (2026-09-12), and A6.15 (2026-09-14) uncritiqued** — A5 is run-namespace mechanics; A6 is ADR-0030 step 3's first half, the hand-authored gateway/identity/network/MCP path and the three defects executing it found; A6.10 is its second half, the operator emitting the serving route; A6.11 is the second review of A6.10 — the route took its port from the wrong revision; A6.12 makes the A2A half literal — a `SendMessage` task completes through the gateway; A6.13 has an agent complete a task by calling an MCP tool through the gateway; A6.15 is the human's T1 — `admission.toolRouteWriters` may attach a route to the Gateway on any listener but the serving one, so a real user can publish a tool |
| 8 | CLI | P1+ | L | r2 — critique **PASS** (`08-…`, `reviews/08-review.md`; awaiting user approval, ADR-0022) |
| 9 | SDK templates | P1 | M | r2 — critique **PASS** (`09-…`, `reviews/09-review.md`; awaiting user approval, ADR-0022) |
| 10 | Observability pack | P1+ | M | r2 — critique **PASS** (`10-…`, `reviews/10-review.md`; awaiting user approval, ADR-0022) |
| 11 | Connector CRD | P2 | M | **HYPOTHESIS (ADR-0030)** · r2 — critique **PASS** (`11-…`, `reviews/11-review.md`; awaiting user approval, ADR-0023); **§11 owed (2026-09-04)** — the `MCPServer` kind and the tool-name uniqueness three designs assume |
| 12 | Ontology spec | P2 | M | **HYPOTHESIS (ADR-0030)** · r2 — critique **PASS** (`12-…`, `reviews/12-review.md`; awaiting user approval, ADR-0023) |
| 13 | Graphiti provider adapter | P2 | M | **HYPOTHESIS (ADR-0030)** · r2 — critique **PASS** (`13-…`, `reviews/13-review.md`; awaiting user approval, ADR-0023) |
| 14 | Ingestion pipeline + extraction QA | P2 | L | **HYPOTHESIS (ADR-0030)** · r2 — critique **PASS** (`14-…`, `reviews/14-review.md`; awaiting user approval, ADR-0023) |
| 15 | Probe engine | P2 | M | r2 — critique **PASS** (`15-…`, `reviews/15-review.md`; awaiting user approval, ADR-0023) |
| 16 | EvalSuite + gate controller | P3 | L | r2 — critique **PASS** (`16-…`, `reviews/16-review.md`), ADR-0024. **First slice approved by the human on 2026-09-14** (ADR-0024 Amendment 1), on Q1–Q13 as recommended; **the rest of the design not approved**. The first slice (§1.1, A3, 2026-09-14): ADR-0030 step 4's narrow gate, blue/green only, one EvalSuite per Agent, whose cases an eval controller in the operator sends as A2A tasks to the candidate's Service before it takes any traffic. Its first critique returned REVISE (`reviews/16-a3-critique.md`: 0 BLOCKER, 8 MAJOR, 9 MINOR), answered by A4; the re-critique returned REVISE (`reviews/16-a4-critique.md`: 0 BLOCKER, 6 MAJOR, 7 MINOR), answered by A5; the third critique returned REVISE (`reviews/16-a5-critique.md`: 0 BLOCKER, 2 MAJOR, 8 MINOR), answered by A6; the fourth critique returned REVISE (`reviews/16-a6-critique.md`: 0 BLOCKER, 1 MAJOR, 4 MINOR), answered by A7; the fifth critique returned REVISE (`reviews/16-a7-critique.md`: 0 BLOCKER, 1 MAJOR, 3 MINOR), answered by A8; the sixth critique returned REVISE (`reviews/16-a8-critique.md`: 0 BLOCKER, 1 MAJOR, 5 MINOR), answered by A9; the seventh critique returned REVISE (`reviews/16-a9-critique.md`: 0 BLOCKER, 1 MAJOR, 6 MINOR), answered by A10; the eighth critique returned PASS (`reviews/16-a10-critique.md`: 0 BLOCKER, 0 MAJOR, 6 MINOR), whose minors A11 closes; the ninth critique returned FAIL (`reviews/16-a11-critique.md`: 0 BLOCKER, 2 MAJOR, 6 MINOR), answered by A12; the tenth critique returned FAIL (`reviews/16-a12-critique.md`: 0 BLOCKER, 2 MAJOR, 6 MINOR), answered by A13; the eleventh critique returned FAIL (`reviews/16-a13-critique.md`: 0 BLOCKER, 2 MAJOR, 8 MINOR), answered by A15; **the twelfth critique returned PASS** (`reviews/16-a15-critique.md`: 0 BLOCKER, 0 MAJOR, 7 MINOR, and "the slice is ready"), whose minors A17 closes. **The human approved it on 2026-09-14, after twelve critiques**, deciding Q1–Q13 as recommended; ADR-0024 Amendment 1 records it. Until A3 the design's own Status line said "approved", which this row never did. **A19 (2026-09-16) pays the follow-up debt to design 03 A77 and A78**, both merged and implemented by PR #48: the missing-policy `Lock` re-points its route at `status.activeRevision` while that revision is carded, and no `Lock` takes a `ProbingAfter` answer until the Gateway reports on the route at its current generation. §1.1 is realigned — the wedge subsection's two opening paragraphs, its "The route", "The Agent's conditions", "The transient exit", "Not exits" and "Leaving it standing" rows, a new row for the ending that needs nobody, the single-`backendRef` narrowing in three places, the exemption's "What it buys", "The wedge is design 03's", the promotion-during-a-`Lock` bullet, Q12's cost, the Design 03 dependency row, Q13's forward-looking note, and five owed envtest rows, none withdrawn. **The wedge is narrower, not gone**: it is the state in which neither the revision the route names nor `status.activeRevision` has a recorded card digest. **No decision among Q1–Q13 moves**, and clause 1 of the exemption is left as it is, under-inclusive and fail-closed: widening it needed the human, it was put to them as a table with leaving it recommended, and **the human decided on 2026-09-17 to leave it — option (a)**. Clause 1 therefore reads today exactly as it read when the slice was approved on 2026-09-14, no rule of §1.1 changes, and ADR-0024 Amendment 1 stays true unamended — which is why **no ADR amendment is added**, a choice rather than an omission: taking (b) or (c) later would need one, the way ADR-0034 Amendment 6 carried design 03's change. All three options and their costs stay in §1.1 as the record of what was weighed, with the fifth critique's preference between the widenings, (c). Their spread is small: the exemption is a conjunction of five clauses and widening clause 1 suspends none of the rest, so clause 4 binds every option, and (b)'s whole excess over (c) is the one state where the route's revision is carded and `status.activeRevision` is not — a `Lock` that credits itself out within a pass or two. **What either widening switches on is clause 5**, whose **four** reads nothing makes for this class today — three uncached, and one cached `Get` of `<agent>-serving` that is clause 5's own, since it is skipped only on an empty run-namespace list and at `ProbingAfter` that list holds `<agent>-auth`. So (c) adds **no** read; it moves the fourth to clause 1, onto passes that never reach clause 5, and that is what closes (b)'s window. The reads run for as long as a candidate is pending, and Q2 (a) holds a candidate on a `Failed` verdict until its owner acts, so on the gate's refusal path they have no bound. **Its first four critiques returned FAIL and the fifth PASSED it** (`reviews/16-a19-critique.md`: 1 BLOCKER, 3 MAJOR, 7 MINOR; `reviews/16-a19-recritique.md`: 0 BLOCKER, 2 MAJOR, 5 MINOR; `reviews/16-a19-critique3.md`: 0 BLOCKER, 3 MAJOR, 4 MINOR; `reviews/16-a19-critique4.md`: 0 BLOCKER, 1 MAJOR, 5 MINOR; **`reviews/16-a19-critique5.md`: PASS**, 0 BLOCKER, 0 MAJOR, 5 MINOR, all applied), and A19 is revised in place on design 03 A77's precedent: the first draft had withdrawn the owed "route and active revision apart" row on the false premise that a carded `status.activeRevision` ends that state, which drops the requirement that a pass reach the re-point — case 18 (c) measures the counterexample. The row is restored and rebuilt, the normative `GatesPassed` cell is rewritten, the dead "promotion during a missing-policy `Lock`" row is rebuilt, and A78's measured-harmful revert warning is un-narrowed. The re-critique then found that the restored row asserted exits §1.1's own rule excludes in the state it builds — with a carded `status.activeRevision` the message names none, and the operator returns early — so those assertions and their mutations move to the row's uncarded variant, the main build asserts the generic message and no exits, and "When it names the exits" loses the justification the BLOCKER had killed and keeps its output. The third critique then found the human's block itself wrong twice — a fourth option that was the second restated, since clause 4 binds every option, and a cost for (c) that §1.1's own costing of clause 5 contradicts — and found the killed inference still standing in the wedge summary bullet and in Q12's decided text, neither of which the earlier rounds had enumerated. The fourth found the read correction applied to (c) and never carried to (b), which had been billed "free" while it is what turns clause 5's three uncached reads on for a class that makes none; the block is now a table, because four rounds each found a defect in the same four paragraphs and every one is a cell in it. The fifth **passed** A19 and closed five minors: clause 5 makes four reads and not three, so the cached one is (b)'s too and (c) adds none; the reads do not stop when the run does, since Q2 (a) holds a candidate on a `Failed` verdict indefinitely; (a)'s contention cell had to say "a **re-creation** `Lock`'s", the J2 and K2 exemption standing under (a); the claim that `runLock` reads the route live is withdrawn, `currentServingRoute` being the cached read; and three citations in (c)'s bullet are re-pointed. **Five independent critics reached (a)**, the fifth adding that between the widenings it would take (c), and **the human took (a) on 2026-09-17**. **A19 also records, without acting on it, that design 03 §8.1 case 18 (h)'s coverage sentence is wrong**: case 18 (c) holds at `ApplyingPolicies` and cannot reach the ordering (h) credits it with, so nothing in design 03's suite pins it — that goes to design 03's own process. A19 also corrects five things A77's inventory missed: clause 4 read cards by revision digest where `cardAttributes` reads them by name; `runLock` now has two `recreateRoute` call sites, not one; "What it buys" credited a J2 or K2 `Lock`'s next `401` in the promoting pass, which A77's route gate defers; a route with two `backendRef`s is now the specific message rather than the generic fallback; and the owed "promotion during a missing-policy `Lock`" row was dead under A77, since the re-point reads `status.activeRevision` and not the revision the route names |
| 17 | Session replay engine | P3 | M | r2 — critique **PASS** (`17-…`, `reviews/17-review.md`; awaiting user approval, ADR-0024) |
| 18 | Pack format + installer | P3 | M | **HYPOTHESIS (ADR-0030)** · r2 — critique **PASS** (`18-…`, `reviews/18-review.md`; awaiting user approval, ADR-0024) |
| 19 | Knowledge pattern packs | P2–P3 | M | **HYPOTHESIS (ADR-0030)** · r2 — critique **PASS** (`19-…`, `reviews/19-review.md`; awaiting user approval, ADR-0023) |
| 20 | Drift controllers | P4 | L | r2 — critique **PASS** (`20-…`, `reviews/20-review.md`; awaiting user approval, ADR-0025) |
| 21 | workflow-operator + Workflow CRD | P4 | M | **HYPOTHESIS (ADR-0030)** · r2 — critique **PASS** (`21-…`, `reviews/21-review.md`; **R2-a required pre-ADR-0025**: missing 06 `workflow-actor` record) |
| 22 | Loop governance | P4 | M | r2 — critique **PASS** (`22-…`, `reviews/22-review.md`; awaiting user approval, ADR-0025) |
| 23 | App layer | P4 | M | **HYPOTHESIS (ADR-0030)** · r2 — critique **PASS** (`23-…`, `reviews/23-review.md`; awaiting user approval, ADR-0025) |
| 24 | AuthzProvider (OpenFGA) | P4 | M | r2 — critique **PASS** (`24-…`, `reviews/24-review.md`; awaiting user approval, ADR-0025; sequenced with 21 R2-a); **A1 (2026-09-03) uncritiqued** — trust domain in the machine subject |
| 25 | model-operator + ModelHub | P5 | L | **HYPOTHESIS (ADR-0030)** · r2 — critique **PASS** (`25-…`, `reviews/25-review.md`; awaiting user approval, ADR-0026) |
| 26 | Tenant CR fan-out | ent | L | **HYPOTHESIS (ADR-0030)** · r3 — critique **PASS** (`26-…`, `reviews/26-review.md`; **R3-a required pre-ADR-0026**: cross-cluster ownerRef lifecycle); **A1 (2026-09-03) uncritiqued** — run namespace ownership per mode |
| 27 | Compliance profile packs | ent | M | **HYPOTHESIS (ADR-0030)** · r2 — critique **PASS** (`27-…`, `reviews/27-review.md`; **R2-a required pre-ADR-0026**: chainer runtime home); **A1 (2026-09-03) uncritiqued** — hard-mode gate integrity is an attested exception |

Update this table when a design starts / lands. Decisions made during design → new ADR.
