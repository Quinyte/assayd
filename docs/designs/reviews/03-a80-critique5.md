# Design 03 (policy compiler): fifth critique of A80, after 2 BLOCKER/6 MAJOR/9 MINOR, 4 MAJOR/8 MINOR, 2 MAJOR/9 MINOR and 3 MAJOR/10 MINOR

- **Date**: 2026-09-18.
- **Reviewer**: an independent adversarial critique, fifth round on A80, following `.claude/skills/critique-design/SKILL.md` and `AGENTS.md`.
- **Target**: `docs/designs/03-policy-compiler.md` and `docs/designs/README.md` at `design-03-served-route-status` `cad5d51`. This round is `git diff 8d3a8c4 cad5d51`; the whole amendment is `git diff origin/main...cad5d51`. The tree diff touches **three files, all under `docs/designs/`** — the design (44 lines), the README row, and the fourth critique's committed record. **Nothing under `internal/`, `api/`, `cmd/`, `test/`, `charts/` or `config/`**, confirmed by `git diff --stat`.
- **Scope**: text only. Case 19's **twelve** envtest rows and its unit table are owed, not built, and are graded on whether they could be built and would be killed. Every claim below was re-derived by hand against the code at `cad5d51`; nothing from the fourth critique's "what I verified" was taken on trust where this round's findings depend on it.
- **Method**: every changed line read in place. Code re-derived by hand: `internal/controller/authtxn.go` (`reconcileGateway` `:276`–`:333` including the capture of `storedHold` at `:304` *before* `gatewayStep` at `:305`, `reportAboveServed` `:354`–`:387`, `seedStoredAbove` `:414`–`:453`, `carriedNote` `:458`, `carriedReason` `:464`, `incompleteOrder` `:487`, `incompleteRank` `:489`, `raiseIncomplete` `:500`–`:512`, `withholdInOrder` `:516`–`:522`, `reportForeign` `:534`–`:562`, `gatewayStep`'s `out.foreign` assignment `:611`–`:617`, `authStep` `:621`–`:653`, `persistStatus` `:1017`–`:1032`, `authKept` `:1035`–`:1046`, `requireFreshAgent` `:1049`, `reconcileServed` `:1498`–`:1544`, `refuseAdopt` `:2284`–`:2337`, every `out.served` assignment), `internal/controller/agent_controller.go` (the rollout switch's `Ready=True` at `:806`, the three post-`reconcileGateway` arms at `:828`, `:849`, `:881`, `withholdReady` `:944`–`:959`, `writeStatus` `:1794`), `internal/controller/conditions.go` (`set`, `get`, `unset`, `carry`, `ownedTypes`, `stickyTypes`, `merge` `:130`–`:168`), `internal/controller/httproute.go` (`assessGovernance` `:708`–`:762`), `internal/controller/authabove.go` (`gatewayAuth` `:78`–`:97`, `capturingListeners` `:316`–`:330`), and a tree-wide search for any read of a Gateway's `status.listeners` (none). A forked `critique-design` pass was invoked; it did not resolve, so everything here is first-hand. No file in the repository was written or changed.

## Verdict: **PASS — 0 BLOCKER, 0 MAJOR, 7 MINOR.**

**The mechanism paragraph is, for the first time, an instruction an implementer can execute.** I did exactly what `03:839`–`03:849` lists and nothing else, and traced the result against §5's two rows and all twelve case-19 rows. Placement, objects, and the four writes are correct and sufficient for (a), (b), (c), (d), (e), (f) including its third half, (g), (h), (j) and (l); the errored-pass bullet and the named caller widening carry (l) and its second mutation; and the rows that need machinery outside the list — (i)'s carry set and (k)'s hold rule — are incorporated by explicit reference to paragraphs that now state them. The pattern the last four rounds found — a property asserted with its machinery left to the reader — is broken.

**There is a fifth piece, and it is one clause, not one mechanism.** The paragraph is a list of *what the judgement writes*. The judgement makes one more write than the list admits: the two claim flags `03:831` introduces one bullet earlier, which the judgement is told to set when it raises a claim and clear when an explicit not-broken tuple clears it. So "all four" is five, and "an implementer who does what is listed here, and nothing else, gets the behaviour … case 19's twelve rows assert" is false for (k)'s third half by exactly that omission. This is not the shape of the previous four rounds: the machinery is written down, in the same subsection, eight lines up. What is wrong is the list's count and the paragraph's claim of self-sufficiency.

**The rest of what I found is the follow-through on that store.** `03:831` is this round's genuinely new mechanism, and it is specified in one sentence and nowhere else: it names no field, it is absent from §3.3.1's `status.auth` block — the design's normative shape for that object — it is absent from §11's "Body changed" inventory, which lists every other body edit down to a recount, it is outside what `authKept` checks, so the `crds/` pruning the same sentence names is silent for it, and it is rewritten wholesale on every pass of a refused `Adopt` by `refuseAdopt`, which is one of the three classes `03:820` deliberately admits. Each is one clause. None of them leaves a state in which an implementer following §3.3.3 as a whole ships an Agent reporting healthy on a refused route: the read-ordering hazard is resolved by the precedent `03:831` itself cites (`storedHold` captures before the step), and the pruning and gating defects cost the duration the page rests on rather than `Ready`. That is why none of them is a MAJOR by the bar the last three rounds applied to themselves.

### What I verified, by trying to break the three closures

- **Closure 1, `Ready` on the errored pass — verified whole.** `reconcileGateway` does **not** return early on an error: it falls through `reportForeign` (`err == nil`) and `reportAboveServed` (`err == nil || transientRouteWrite(err)`) to `return out, err` (`authtxn.go:323`–`:332`), so `03:841`'s placement is reachable on an errored pass and `03:848`'s restoration can live there. The three arms are as `03:849` describes them: `:828`'s non-transient arm never calls `withholdReady`; `:849` needs `tx != nil` and `TxCreate`/`TxLock`; `:881` is gated `w.reason == ReasonGatewayAuthPolicy`. `withholdReady` (`:944`) is the sole writer of `Ready=False` on this path and the rollout switch sets `Ready=True` ten lines above the call (`:806`), so `03:846`'s claim holds.
- **The widening's reach is exactly right for every standing reason, which I enumerated rather than assumed.** For the judgement's class on a transient errored pass, `reportForeign` does not run, so the only reason the `-auth` step can leave beside A80's two is `GatewayAuthPolicy` from `reportAboveServed` (`:374`), which the arm already admits. `withholdInOrder` ranks by `incompleteRank`, so with `GatewayAuthPolicy` (rank 2) standing a restoration under rank 3 or 4 does not displace it and the existing gate fires; with nothing standing the restoration's own reason is the withhold and the widened gate fires. A stored `ForeignTrafficPolicy` cannot be the withhold on that pass, because `reportForeign` is gated on `err == nil`. So "either of its new reasons" is neither too narrow nor wider than A80 owns. The narrower widening is the right call over the fourth critique's "any withhold", which would have moved behaviour for a reason A80 does not own.
- **Case 19 (l)'s second mutation kills what it claims.** (l)'s state is (a)'s — served `apikey`, empty slot, no Gateway-level policy — so the restored withhold's reason is `ServingRouteNotAccepted`. Left gated on `GatewayAuthPolicy`, arm 3 does not fire, `withholdReady` is not reached, and `Ready` keeps the `True` `:806` set; the first half's `Ready=False` assertion fails. One compiling edit, and it is the pin on the one change outside the judgement.
- **Closure 2, `gatewayOutcome.served` — verified.** Seven setters exist, six in `authtxn.go` (`:375` `reportAboveServed`, `:560` `reportForeign`, `:699` and `:950` the abandon paths, `:1350` `runCreate`, `:1892` `runLock`) and one inline in the caller's first arm (`agent_controller.go:878`). `reconcileServed` spans `:1498`–`:1544` and sets it nowhere. In (a)'s state `reportAboveServed` returns at `!above.stands()` (`:361`) before `:375`, and `reportForeign` returns on `len(out.foreign) == 0` (`:536`) before `:560`, so `03:847`'s "neither reporter beside the judgement reaches its own setter" is exact. `withholdReady`'s second decision is at `:953`–`:959`, and `CondDegraded` is in `ownedTypes` and not in `stickyTypes`, so `merge`'s owned arm clears it — the paragraph's "actively cleared" is right.
- **(a)'s new mutation is single, compiling and fatal to the row.** Leaving `served` unset gives phase `Pending` and no `Degraded`, against (a)'s `phase Degraded`. It is one field.
- **Closure 3, the claim rule per condition — the diagnosis and both halves are right, and I could not reopen either.** `GovernanceSkipped` is in both `ownedTypes` and `stickyTypes` (`conditions.go:126`), so a stored value always exists; `assessGovernance` derives `False`/`AuthVerifiedOnOneReplica` for a served `apikey` Agent through `governanceFor` (`httproute.go:749`–`:752`); `seedStoredAbove` already restores that condition **whole** over `assessGovernance`'s fresh value, for `ForeignTrafficPolicy` (`authtxn.go:428`–`:437`) and for W1's `GatewayAuthPolicy` (`:439`–`:452`). So `03:830`'s premise, its precedent and its fail-open warning are all correct, and the fourth critique's MAJOR 3 is answered on its own terms. `raiseIncomplete` does hardcode `CondPolicyApplyIncomplete` at all four writes (`:501`, `:504`, `:507`, `:510`), so naming a separate composition for `GovernanceSkipped` is necessary and not belt-and-braces.
- **The `LastTransitionTime` arithmetic the rule rests on is real.** `merge` keeps `prev.LastTransitionTime` whenever the **status** is unchanged, whatever the reason does (`conditions.go:153`–`:157`), so `03:829`'s "if no other reason stands it becomes the reason and keeps the stored `LastTransitionTime`" is true through `conds.set`, and `carry` additionally keeps `ObservedGeneration`. The distinction `03:829` draws between the two halves — both keep the timestamp, only the whole restoration keeps the generation — is precise and is what the code does.
- **(k)'s fourth half kills what it claims.** Under the mutation "re-append to whatever the pass derived instead of re-asserting whole", the unknown pass leaves `GovernanceSkipped=False`/`AuthVerifiedOnOneReplica` with the claim in the message; the half asserts `True`/`AuthPolicyNotAttached`, and the status flip also moves `LastTransitionTime`, so both assertions fail. One branch swap, and it compiles.
- **The composition order is order-independent and the placement that makes it work is stated.** With both halves firing, `raiseIncomplete` ends on `AuthPolicyNotAttached` (rank 3) whichever order the two calls are made in. `reportAboveServed`'s W1 branch overwrites `GovernanceSkipped` unless a `ForeignTrafficPolicy` stands, so the judgement must run **after** both reporters for `03:845`'s "keeps the reason and appends" to be reachable — `03:855` says "the judgement runs last, under the placement above", so it is written down, though the placement bullet's own word is "beside".
- **The refused `Adopt` is genuinely in the fragment case, which matters for MINOR 3.** `gatewayStep` sets `out.foreign` before dispatch and re-attaches it to whatever `authStep` returns (`:611`–`:617`), so `refuseAdopt`'s empty `gatewayOutcome{}` still carries `foreign`, and `reportForeign` runs for that Agent, as its own comment says. `ForeignTrafficPolicy` is rank 1 and outranks `ServingRouteNotAccepted`, so the claim is a fragment there.
- **Choice A's baseline survived a third independent attack.** A tree-wide search finds no read of a Gateway's `status.listeners` anywhere in `internal/`, `api/` or `cmd/`; `gatewayAuth.listeners` is the ListenerSet summary and carries no listener names (`authabove.go:92`–`:93`); `capturingListeners` iterates `g.Spec.Listeners` twice on the pass's own read and returns a map it discards (`:316`–`:330`). A4-spec is the only reading on the table that survives a dead gateway controller, the cell says so, and the owed-return cost is now stated.
- **Rule 7 on the page claim still holds.** No alert rule, `PrometheusRule` or dashboard in `charts/` or `config/`; every page claim in A80 is conditional; `03:1508` now rests the caveat on **not built** and records the design-10 Status-line/README contradiction as owed to `audit-docs` rather than picking a side. That is the right answer to the fourth round's MINOR 1.
- **All ten of the fourth round's minors are closed.** MINOR 2 at `03:1493` (the owed return, now at both sites); MINOR 3 in the `(A4)` cell (the owed signature); MINOR 4 in the same cell ("All of that is owed with the option"); MINOR 5 at `03:1446` (the perturb-then-fail step, or `errStaleAgent`, which `requireFreshAgent` does evaluate on every gateway pass at `:1049`); MINOR 6 at `03:849` and `03:1480` and `03:1508` (the composition's reach over every standing reason); MINOR 7 at `03:1520` (recounted to ten, with a fourth-revision sentence); MINOR 8 in `README.md` (the spec reading named); MINOR 9 at `03:835` (three markers) and `03:1364` (the policy row's two clauses); MINOR 10 (the stray backtick is gone — zero occurrences remain).

---

## MINOR findings

1. **The list says "all four" and the judgement makes five: the claim store is a write, and it is not on the list.** `03:843`–`03:847`, against `03:831` and case 19 (k)'s third half at `03:1445`. `03:831` tells the judgement to set two flags when it raises a claim and clear them when an explicit not-broken tuple clears it. That is a write of this judgement, on the same pass, and `03:839`'s framing — "does what is listed here, and nothing else" — is falsified by it: (k)'s third half needs a flag no listed item writes. **Fix.** A fifth numbered item: the two claim flags on `status.auth`, set when either reason is raised and cleared when an explicit not-broken tuple clears it, so the fragment rule above is computable on the next pass. Change "all four" to "all five" in the lead and in `03:1520`.
2. **The store has no field names, no place in §3.3.1's `status.auth` block, and no line in §11's inventory — and it is a CRD addition.** `03:831`, against `03:489`–`03:522` and `03:1520`. Every other field of `status.auth` is named, typed and given its presence rule in the block at `03:489`; A80's two are described only as "two booleans on `status.auth`, one per reason". An implementer building the CRD type from the design has no names and no optionality to work from, nothing in case 19 references them, and `03:1520` — which lists §1.1's row, §3.1's recount, ten paragraphs, two §5 rows and twelve envtest rows — records no API change at all. The `crds/` hazard the same sentence names exists *because* these are CRD fields, so the schema block is exactly where they belong. **Fix.** Two lines in the `status.auth` block with names and presence rules, and one clause in `03:1520` recording that §3.3.1's status schema gains two fields.
3. **`refuseAdopt` rewrites `status.auth` wholesale on every pass, so the flags must be captured before the `-auth` step, and `03:831` does not say so.** `03:831`, against `internal/controller/authtxn.go:2308`, `:1017`–`:1022`, `agent_controller.go:1797`. For a refused `Adopt` — one of the three classes `03:820` admits, and the one it calls least covered — `refuseAdopt` assigns `status.Auth = &AuthStatus{Transaction: …}`, discarding every other field, and `persistStatus` writes it and `writeStatus` sets `agent.Status = *next` **before** the judgement runs. An implementer who reads the flags where they are used — at the judgement, after the step — reads them wiped on every pass, and the store is inert for that class; beside a foreign `traffic` policy, whose `ForeignTrafficPolicy` outranks `ServingRouteNotAccepted`, the fragment is then never held, and removing the foreign policy on a pass whose route reading is unknown returns the Agent to `Ready=True` on a refused route. An implementer who follows the precedent `03:831` itself invokes — `storedHold` is captured at `authtxn.go:304`, before `gatewayStep` — gets it right. That the fix is already the house pattern is why this is a MINOR and not more. **Fix.** One clause: the flags are read from stored status before the `-auth` step, as `storedHold` is, because `refuseAdopt` rebuilds `status.auth` on every pass of a refused `Adopt`.
4. **`authKept` does not check the new fields, so the `crds/` pruning the same sentence names is silent for them.** `03:831`, against `internal/controller/authtxn.go:1035`–`:1046`. `authKept` compares `Mode` and, when a transaction is present, `Kind`, `Stage`, `Written` and `TargetMode`. A CRD that predates A80 — which is every CRD in the field, and `helm upgrade` never updates `crds/` — keeps all of those and prunes the two new booleans without an error, so `persistStatus` returns nil, the operator goes on writing to the gateway, and the hold silently degrades to the fail-open behaviour §3.3.3 exists to prevent. The sentence's "`authKept` already refuses to write to the gateway when `status.auth` does not come back" is true of `authKept` today and reads as cover for the new fields, which it is not: that is rule 7 in the small, and rule 8 on the outcome. **Fix.** One clause: either `authKept` gains the two fields, so a pruning CRD is refused as `AuthRecordNotKept` like any other lost record, or say in terms that it does not and that the pruning is silent, and cost that.
5. **"Both reasons join A75's carry set" is under-specified for two of the three classes the judgement admits.** `03:853`, against `internal/controller/authtxn.go:414`–`:453` and case 19 (i) at `03:1443`. `seedStoredAbove` is three blocks with three different gates: the `storedHold` block needs a transaction at `ProbingAfter`, the `ForeignTrafficPolicy` loop is ungated, and the W1 block returns unless `auth.Mode == apikey` **and** `auth.Transaction == nil` (`:439`). An implementer who adds the new reasons to the W1 block — the nearest sibling — carries nothing for a served `auth: none` Agent or a refused `Adopt`, both of which the route half covers, so a pass that returns before the `-auth` step drops their `ServingRouteNotAccepted` and resets the clock the page rests on. Case 19 (i) is written only for (a)'s state and its policy half, so no owed row catches it. **Fix.** One clause at `03:853` saying the carry is a block of its own, gated on the judgement's classes rather than on `mode == apikey`; and one clause in (i) adding a served `auth: none` Agent or a refused `Adopt`.
6. **§5's route row lost the symmetry its twin just gained.** `03:1363` against `03:1364`. The policy row now reads "**A pass whose `-auth` step errored holds it the same way**, repopulating the withhold and the served flag with it"; the route row reads "**A pass whose `-auth` step errored holds it the same way**, transiently or not" and stops. The fourth round's MINOR 9 asked for these two rows to state one rule; they now state it at two different depths, in the other direction. **Fix.** Carry "repopulating the withhold and the served flag with it" into `03:1363`.
7. **`03:1508`'s closing sentence is still understated for (A4), which the same cell now contradicts.** "a different answer is a one-clause edit to §3.3.3 and §5 rather than a redesign" — while the `(A4)` cell says the option needs a reason name, a rank in `incompleteOrder`, a §5 row and a return `gatewayAuthPolicies` does not have, and calls all of it owed. The cell is right and the closing sentence has not been reconciled with it. The fourth round's MINOR 4 is half closed. **Fix.** One clause: "(A4) excepted, which the cell above costs".

---

## On the framing put to the human, choice by choice

**The shape has been right for four rounds** — *how*, never *whether*; every option costed; every cell recommending; (c), (d), (f), (h), (k) and (l) pinning the recommended call against the obvious alternative. All three choices are fit. Nothing owed from a major, because there is no major.

- **Choice A — fit.** The baseline is right and now for a stated reason rather than by elimination, and it survived a third attack: I re-derived that nothing in the operator reads a Gateway's `status.listeners`, that `capturingListeners` iterates `spec.listeners` on the pass's own read and discards the names, and that `gatewayAuth` carries no listener names, so A4-spec is the only reading here that survives a dead gateway controller and A4-status survives nothing (A1) does not. **A80 is right to leave (A4)'s reason name, rank and §5 row unwritten, and the cell is now what makes that right**: the human is deciding whether to *add* an option, not how to spell it, and the cell states the four owed pieces plainly — "All of that is owed with the option". Naming a reason and writing a §5 row for an option that may not be taken would put a guarantee in §5 that nothing is committed to. It is decidable as it stands. The only thing owed here is MINOR 7's clause, so that `03:1508` stops promising a one-clause edit the cell has already priced higher.
- **Choice B — fit, and the round's improvement lands here.** (B1) is unchanged and still matches W1 exactly. What was unsettled last round — what holds (B1)'s report on `GovernanceSkipped`, the condition (B1) exists to raise — is now settled correctly and in the fail-safe direction: restored **whole** on `seedStoredAbove`'s own precedent, which I verified is shipped code for two reasons already, with only a fragment under another reason re-appended. The fail-open reading is closed. **Caveat 1 remains correctly and prominently stated** — the policy half is inferred, no fixture anywhere shows an accepted route beside an unattached policy, and the `make conformance-cluster` case that would measure it is named and marked owed. (B2)'s and (B3)'s costs are true as stated. The human should know that the store the new rule needs is the subject of MINORs 1–4, but none of them changes what (B1) buys.
- **Choice C — fit, nothing owed.** Unchanged this round and I attacked (C1) again without moving it. (C2)'s cost is republish latency plus a possibly-unbounded A75 hold, not a permanent outage; (C3)'s is the outage switch. Both correct.
- **The ADR claim is right, and its scope sentences are now honest.** (B2) reverses I1 and §3.3.1; (C3) reverses the never-withdraw rule; (A2′), (A2), (A3) and (A4) reverse nothing. A1/B1/C1 needing no ADR is right on the reversal question, and `03:1480` and `03:1508` now both name the two mechanisms that do move — the caller's third error arm and the non-transient arm's composition — with the second one's reach over **every** standing reason spelled out, which is the fourth round's MINOR 6 closed in both places rather than one.

**Claims a human decides on that I could not verify, and none is the drafter's fault.** The policy half is inferred and not measured, and the design says so at the point of decision. The five-minute window is a paragraph in a design that is not built, and whether it is also unapproved is a live Status-line/README conflict the amendment now records rather than settles — the right call.

## What would make this a clean PASS

Seven clauses. Add the claim store as the fifth write, name its two fields in §3.3.1's `status.auth` block and record the API addition in §11's inventory, say the flags are read before the `-auth` step because `refuseAdopt` rebuilds the record, say whether `authKept` gains them, gate the new carry on the judgement's classes rather than on `mode == apikey`, carry the withhold clause into §5's route row, and except (A4) from `03:1508`'s one-clause promise. None of them reverses anything, none needs a human, and none blocks approval.

**The pattern §11 records has broken, and that is worth saying as plainly as the four rounds that established it.** For four rounds the mechanism paragraph asserted an outcome and left out the one piece of machinery that decided whether the Agent reported healthy, and each time the piece was written down nowhere. This round it is a list of writes, and every piece it needs is written down — the withhold, `served`, the per-condition restoration, the composition, the widened caller arm. The fifth piece I found is on the list's count, not in the design's content, and everything else I found is the paperwork the new store still owes. That is a different kind of finding, and it is what a fifth round should be returning.
