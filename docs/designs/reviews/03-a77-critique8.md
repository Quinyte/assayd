# Design 03 (policy compiler): eighth critique of A77, scoped to the first slice (§1.1)

- **Date**: 2026-09-16.
- **Reviewer**: an independent adversarial critique, following `.claude/skills/critique-design/SKILL.md` and `AGENTS.md`. This is the **eighth** critique of A77. The first returned FAIL (1 BLOCKER, 6 MAJOR, 13 MINOR); the second FAIL (0 BLOCKER, 4 MAJOR, 10 MINOR); the third FAIL (0 BLOCKER, 5 MAJOR, 8 MINOR); the fourth FAIL (1 BLOCKER, 4 MAJOR, 7 MINOR); the fifth FAIL (0 BLOCKER, 3 MAJOR, 7 MINOR); the sixth FAIL (0 BLOCKER, 2 MAJOR, 8 MINOR); the seventh FAIL (0 BLOCKER, 3 MAJOR, 9 MINOR, two of the MAJORs in the then-new research note).
- **Target**: `docs/designs/03-policy-compiler.md`, `docs/decisions/0034-slice-auth-follows-current-spec-narrows-only-on-a-probe.md`, `docs/designs/README.md`, `AGENTS.md`, `CLAUDE.md` and `docs/research/auth-lock-false-credit-2026-09.md` at `design-03-lock-repoint` `d093b5c`. This round's revision is `74e053f..d093b5c`; the whole amendment is `origin/main...d093b5c`.
- **Scope**: design 03's first slice is approved and implemented (ADR-0034 Amendment 4, slice PRs 4 and 5). A77 changes shipped behaviour, so it is held to the standard for an amendment to an approved design. This round checks the re-run research note against the code, the three MAJOR closures, the nine minors, and anything the closures themselves wrote.
- **Method**: every hunk in `74e053f..d093b5c` read in place and then the whole line it sits in read as prose — the design's changed lines are `03:3`, `03:767`, `03:776`, `03:777`, `03:1321`, `03:1422`, `03:1424`, `03:1432`, `03:1451`, `03:1454` and the new `03:1455`; the ADR's Amendment 6 header, its critique list and its terminal-message bullet; the README's design 03 row. The research note read start to finish and each claim re-derived against the code. Code re-derived by hand: `internal/controller/authtxn.go` (`enterLock`, `lockMissingPolicy`, `runLock` end to end including the `StageProbingBefore`, `StageApplyingPolicies` and `StageConverging, StageProbingAfter` arms and the tail, `lockServed`, `lockPendingMessage`, `reconcileServed`, `recreateRoute`, `refuseAdopt`, `cardAttributes`, `beforeRevisionFor`, `routeConverged`, `policyConverged`, `storedHold`, `seedStoredAbove`, `carriedReason`, `reconcileGateway`'s seed withdrawal and its restore arm, `reportAboveServed`, `governanceFor`, `governanceReason`, `AuthTransactionDeadline`), `internal/controller/authnack.go` (`GenuineNack`, `eventLastSeen`, `policyWrittenAt`, `freshNack` and its message verbatim), `internal/controller/httproute.go` (`ensureServingRoute`, `currentServingRoute`), `internal/compiler/auth.go` (`KeySource`). The note's verbatim output was checked against the real format strings it quotes. Every helper the note names as pre-existing was looked up in `test/envtest`, and the one it names as new was confirmed absent. `v0.4.0`, `0303acb` and `3f37c25` compared commit by commit and file by file. `AGENTS.md:13` and `CLAUDE.md:11` compared by checksum. `git diff origin/main...d093b5c -- internal/ api/ cmd/ test/ charts/ config/` is empty, confirmed independently. Critique counts swept across the Status line, §11, the README row and the ADR. A `critique-design` pass could not be invoked in this session (the skill's `paths:` key had not resolved); the work below is entirely hand-derived, and each finding names the code it rests on. Nothing was run against a cluster. No file in the repository was written or changed.

## Verdict: **PASS — 0 BLOCKER, 0 MAJOR, 8 MINOR.**

**All three of the seventh critique's MAJORs are closed, and all nine of its minors, and I could not break any of the three closures.** The decision that shaped this round — to re-run the reproduction rather than argue about it — was the right one and it shows: the note's evidence is now first-hand, the table agrees with the fixture, and the parts that were inferred are labelled as inferred. What remains is eight one-sentence corrections, none of which touches a rule, a condition, a message, a decision or a test, and none of which an implementer would be misled by for longer than the sentence it sits in. This amendment is implementable as written.

What I verified closed, by trying to break each:

- **Seventh MAJOR 1 (the table contradicted its fixture) is closed, and the corrected run is the one the code produces.** `401 → 401 → 401` is exactly what `runLock` gives under that fixture. Pass 1: `beforeRevisionFor` returns `""` for any answer that is not a `200` with a matching recorded digest, so `beforeObserved` stays unset — the note's new gloss ("which needs a `200` whose digest matches a recorded one") is the function. Pass 2: `cardAttributes` walks the route's `backendRefs` against `status.Cards` and finds no digest for r1, `observed` is false, `creditable` is false, and the tail writes the "cannot be attributed" message. Pass 3: `ensureServingRoute` at the top of `runLock`'s non-re-creation branch rewrites the route onto r2, `rt` is the post-write object, `cardAttributes` finds r2's digest and returns attributed, and `lockServed` records `apikey`. **The verbatim block is the strongest thing in the note**: its `GovernanceSkipped` line reproduces `governanceFor`'s format string character for character, including "on 1 of an unknown number of declared gateway replicas"; its `KeySource` matches `compiler.KeySource`'s `<ns>/<label>=<value>` shape; `AuthTransitionNotBuilt` and `AuthVerifiedOnOneReplica` are both real reasons. That is output no one reconstructs from memory. **The re-run also made the finding sharper, not weaker** — the same answer refused at pass 2 and credited at pass 3, with only the route's name changing — and the note says so.
- **Seventh MAJOR 2 (the NACK's three windows) is closed and each cost is right.** *While it stands*: `freshNack` returns non-empty, `out.nack` is set, the stage is rewound to `Converging`, and the tail's `deadlinePassed || out.nack != ""` arm raises `PolicyApplyIncomplete=AuthLockUnverified` — reason lost, page kept. *At expiry*: the deadline is `AuthTransactionDeadline = 4 * time.Minute` and `freshNack` counts only an Event last seen strictly after `policyWrittenAt`, which is later than the transaction's entry, so the gap is 56 minutes **at the earliest** and larger the later the Event arrives — the design now says exactly that, closing the seventh round's MINOR 8 in the same clause. *Deleted before the deadline*: `freshNack` returns `""`, the `Converging` arm breaks on `routeConverged`/`policyConverged` before A75's read, `held` is false, neither tail arm fires, `out.withhold` stays nil, and `reconcileGateway`'s restore is gated on `err != nil && !set` so nothing puts the seed back — the Agent reads `Ready`. I also checked the seed itself: `storedHold` requires `tx.Stage == StageProbingAfter`, and the NACK pass rewound to `Converging`, so there is nothing to carry either way. **"The first release `freshNack`'s own message names" is accurate**: the message reads "To release the hold, delete that Event, or wait for the API server to expire it", deletion first.
- **Seventh MAJOR 3 (the exemption's second reason) is closed in both places.** The note now gives one reason and it is the structural one — the branch sets `rt = cur` and never calls `ensureServingRoute`, which I re-derived — and the parking is demoted to a labelled observation with the harness fact that caused it. `03:1424`'s "which parks at `Converging` and sends no probe" is gone, replaced by the structural clause. The shared `AGENTS.md`/`CLAUDE.md` sentence now rests on the same reason the note keeps.
- **All nine of the seventh round's minors are closed.** `03:1422` says seven and lists seven; `03:1451` says "four places, two of them written in the sixth round" and cites **A75**'s Gateway-level merge, not A74's; the terminal-message wording is now identical in `03:767`, `03:799`, `03:1321`, `03:1432` **and** the ADR's "The terminal message" bullet; `03:776`'s gate bullet names both movers and `ensureServingRoute`'s position at the top of `runLock` is where the bullet says it is; `03:767` and `03:1321` now say "needs neither exit" and concede the administrator's delete still ends it; the note's Method names the real seam (`AuthProbe`, a public `AuthProber` field, injected at `test/envtest/authcreate_test.go:255`) and claims no `CardDriftInterval` edit.
- **The Method is reproducible from the note alone.** Every one of the fifteen helpers it names as pre-existing exists in `test/envtest` (`promote`, `lockPass`, `reconcileOnce`, `acceptRoute`, `acceptPolicy`, `deletePolicy`, `markAvailable`, `recordCardFor`, `forgetCards`, `toMode`, `mustEdit`, `servingRoute`, `txOf`, `authOf`, `condition`), and the one it names as new, `acceptedGeneration`, does not. That is a checkable inventory, and it checks out. The decision not to commit the fixture is correct and correctly reasoned: it asserts the defect, so it would be permanently red, and the note names §8.1 case 18 (f) and (g) as the tests that pin the corrected behaviour instead.
- **Measurement is separated from inference throughout, including at the two places the round asked about.** "**What was not measured**: that the `Lock` then reaches `Served`" is stated in the fix section where a reader would otherwise assume it, and the rejected variant is now under "**Not measured, and stated because the design rests on it**", with its argument attributed to `persistStatus` and `equalRoute` rather than to a run. That is the right discipline and it is the thing the note exists to model.
- **The shared clause is byte-identical in both files** (same checksum), unchanged this round, and still true: the mechanism, "for J2 and K2", and "not reachable for the missing-policy `Lock`, which writes no route" all hold against `runLock`.
- **No tree code changed.** `git diff origin/main...d093b5c -- internal/ api/ cmd/ test/ charts/ config/` is empty, and so is the same diff against `3f37c25`.

---

## MINOR findings

1. **The note's table puts the policy write one pass too late.** Pass 2's cell reads "`ApplyingPolicies` writes `<agent>-auth`; `Converging` holds §3.3.2's tuple; `ProbingAfter` probes". It cannot have. `enterLock` persists the transaction at `ProbingBefore` and calls `runLock` in the same pass; `runLock`'s `ProbingBefore` arm sets `ApplyingPolicies` and falls through the `for`/`switch` with no return, so pass 1 probes, writes the policy, reaches `Converging`, fails the tuple on a policy with no status yet, and ends there. The table's own pass-2 harness cell — "accepts route and policy at their current generations" — presupposes this, since a policy must exist to be accepted. Nothing downstream moves: the probe count, the generations, the attribution and the verbatim output are all unaffected. *(I considered grading this MAJOR, because the seventh round graded a table inconsistency MAJOR and this is the same family. I do not, and the difference is what a reader can do with it: that round's `200` cells made two rows of the table unreconstructible and left the reader unable to say which run happened, whereas here every answer, generation and credit is stated consistently and independently corroborated by the run's own output, and one stage label sits one row late.)* **Fix.** Pass 1: "…and does not set `beforeObserved` …; `ApplyingPolicies` then writes `<agent>-auth` and the pass ends at `Converging`, where the tuple has not held." Pass 2: "resumes at `Converging`, which now holds §3.3.2's tuple; `ProbingAfter` probes…".

2. **The provenance sentence is right about the tag and wrong in its own absolute.** "two commits after `v0.4.0` (`0303acb`)" is exactly right — `0303acb` → `17e46b7` → `3f37c25`. "from which the operator source **does not differ at all**" is not: `api/v1alpha1/agent_types.go` differs, and the same sentence admits it three clauses later with "and a CRD doc comment". This is the shape the seventh round flagged in the Method paragraph — an absolute its own sentence disproves — and it is now in the sentence that replaced it. The file list is also slightly off: the intervening diff is `AGENTS.md`, `CLAUDE.md`, `api/v1alpha1/agent_types.go`, `charts/assayd/crds/`, `config/crd/` and two files under `docs/`, which is not "`docs/`, `config/` and a CRD doc comment". **Fix.** "…two commits after `v0.4.0` (`0303acb`), from which the operator differs only in a doc comment on one CRD field; the rest of the intervening diff is `docs/`, the two root guidance files and the two generated CRDs."

3. **The note still says A61 "clears" what it never reaches.** Pass 3's cell: "A61 clears `beforeObserved`/`beforeRevision`, which were never set". The block is gated on `tx.BeforeRevision != ""`, so with `beforeRevision` empty it is skipped and nothing is cleared. `03:1424` states it correctly ("A61's clearing is gated on `tx.BeforeRevision != \"\"` and touches only the observed branch"); the note should match it. This is a survivor of the pre-re-run table, not something the re-run introduced.

4. **The reading count moved in one file and not the other.** The note now says "**Four** independent readings of the code reached the same conclusion"; `03:1424` still says "**three** reviewers re-derived it independently from `runLock`". Both are in text citing the same fact, one sentence apart in the reader's path from the design to the note.

5. **`03:1455` misnumbers the finding it answers, and never narrates the one it also fixed.** The NACK finding is the seventh critique's **MAJOR 2**, not "the third MAJOR"; MAJOR 3 was the missing-policy exemption's second, non-structural reason. That one *was* fixed, in the note and at `03:1424`, and the bullet's tag claims "*(Seventh critique, MAJORs 1–3.)*" — but the bullet's prose accounts for MAJOR 1, the two minors, and MAJOR 2 under the wrong number, and says nothing about MAJOR 3. §11 is where a later reader learns what each round found; this is the one place in the amendment where that record is wrong. **Fix.** Call the NACK "its MAJOR 2", and add a clause: "and its MAJOR 3 was this note's own exemption argument, which gave a structural reason and then a second one that was not — the missing-policy `Lock` writes no route, so `routeConverged` compares a generation the Gateway has already reported and passes; the parking is now reported as an observation of that run, with what held it, in the note and at `03:1424`."

6. **`03:777`'s three labels do not cover an Event deleted after the deadline.** The labels are "While it stands", "If it expires", and "If it is deleted before the deadline". A NACK Event deleted *after* the deadline is covered only by a clause tucked inside the expiry bullet — "and from the deadline onwards the same arm raises `AuthLockUnverified` anyway" — which is true and correctly costed, but a reader counting the enumeration the paragraph announces ("all three ways matter") will not find it. **Fix.** Lift the generalisation out of the expiry bullet, as the seventh critique's own proposed sentence did: "…and from the deadline onwards, whatever becomes of the Event, the same arm raises `AuthLockUnverified`."

7. **`03:1422`'s list is still missing one comma; it has moved.** The seventh round recorded it before `reviews/03-a77-critique5.md`; that one was added, and the list now runs "…the sweep's own rewrites had introduced) `reviews/03-a77-critique6.md` (…". Trivial, and the only thing left of a finding that has now been raised twice.

8. **The note's "in the same pass" is ambiguous in the one sentence that carries the exemption's caveat.** "A missing-policy `Lock` entered on a settled route does leave `Converging` in the same pass and probe" reads naturally as "the pass that enters it", which is never true: `lockMissingPolicy` enters at `ApplyingPolicies`, writes a fresh `<agent>-auth` in that same pass, and `policyConverged` refuses a policy that reports no ancestors yet. The intended claim — that once at `Converging` with the tuple met, `runLock` `continue`s into `ProbingAfter` and probes without ending the pass — is right, and it is what the argument needs. **Fix.** "…does leave `Converging` and probe without ending the pass, once the tuple holds."

**Noted, not counted.** `03:63`'s out-of-slice row title still lists "the ordering of auth transactions around a weight shift" while its own cell and `03:49` put A77's gate inside the slice; this is now the fourth round to decline to count it, and its cell still resolves it. The oracle's advance rule is described more loosely than in the previous draft ("which the test advances the way a converged gateway would" replaced a statement keyed to the route's `metadata.generation`); the table pins the behaviour at each pass, so the fixture is still rebuildable, and the loss is one sentence of precision rather than a gap. The corpus-wide split between `research/…` and `docs/research/…` is unchanged and remains the corpus's to fix. The note's `Re-verify` line is condition-based with no date, as two of the comparable notes are.

---

## The questions, answered

**(1) The research note.** The corrected sequence is coherent with the fixture and with the code: I re-derived all three passes against `beforeRevisionFor`, `cardAttributes` and `runLock`'s `ProbingAfter` arm and they give `401 → 401 → 401` with the credit at pass 3, for the reasons the note gives. Measurement is separated from inference throughout — the two things the round asked about are both explicitly marked unmeasured, in the places a reader would otherwise assume them, and the code readings are still called "evidence and not proof". The Method is reproducible from the note alone: the seam is real and already used by the suite, the fifteen named helpers all exist, the one new helper does not, and the fixture's two revisions and the absence of Gateway-level and foreign policies are stated. The provenance sentence is right that `3f37c25` is two commits after `v0.4.0` and wrong that the operator source "does not differ at all" — it differs by a doc comment, which the same sentence names (MINOR 2). One stage sits in the wrong row (MINOR 1) and one carried-over clause describes a clearing that never runs (MINOR 3).

**(2) MAJOR 2, the NACK.** Verified link by link against `freshNack`, `runLock`'s `Converging` arm and tail, `reconcileGateway`'s seed withdrawal and restore, `storedHold` and the reason constants. Each of the three windows is correctly costed, the arithmetic is right and now stated as a floor, and "the first release `freshNack`'s own message names" is accurate to the message's word order. The only gap is a labelling one: deletion *after* the deadline is covered by a clause rather than a label (MINOR 6).

**(3) MAJOR 3, the missing-policy exemption.** True. The branch sets `rt = cur`, never calls `ensureServingRoute`, and so never moves a backend under its own probe — that is the whole reason and it is sufficient. The parking is now separated, attributed to the fixture's ordering, and followed by the caveat that a `Lock` entered on a settled route does reach `ProbingAfter`, which it must for A77's own re-point and for case 18 (a) to exist. `03:1424` carries the corrected clause. Only the caveat's "in the same pass" is ambiguous (MINOR 8).

**(4) The nine minors.** All nine closed. The gate bullet now names both movers and is right about both: the re-point is `03:775`'s step 1, and the promotion is `ensureServingRoute` at the top of `runLock`'s non-re-creation branch, which is where the case A77 exists to fix comes from. `equalRoute` still makes a no-drift write a no-op, so "a settled route is probed on the same pass that re-asserts it" holds. The terminal-message wording is now identical in all five places including the ADR, which was the file-boundary miss. The comma nit survives, relocated (MINOR 7).

**(5) Anything new, and rules 7 and 8.** New: MINORs 1, 3, 4, 5 and 6; MINOR 2 is the new form of a closed finding and MINOR 7 and 8 are residues. **Rule 7**: the amendment's absolutes were swept again. The ones previous rounds found are gone and their replacements hold — "never the page" is now three costed windows, "neither exit applies" is now "needs neither exit", "fifty-six minutes" is now a floor, "the reason is structural" is now one reason. The single survivor of the shape is MINOR 2, in the note, and it is one clause. **Rule 8**: nothing in this round's text drops or misnames a page. The three NACK windows each say what happens to `PolicyApplyIncomplete` and why; the missing-policy `Lock`'s `AuthPolicyMissing` really is set before either tail arm; the terminal message names both exits and which the reported state admits; and the note's own "What no later pass corrects" is honest that a falsely credited Agent reads `GovernanceSkipped=False` and is reported as governed. **The shared clause is byte-identical in both files and true.**

---

## Where the work is right

The decision to re-run rather than reason is the thing to keep. Seven rounds of this amendment have been argued from code readings, and the one artifact that was written up from a run is now the only part of it a reviewer cannot push back on with a different reading — I checked the verbatim block against the format strings in `governanceFor` and `compiler.KeySource` specifically to try to catch a reconstruction, and it is not one. The helper inventory in the Method is the same move at a smaller scale: it turns "trust me, this was reproducible" into fifteen names a reviewer can grep for, and all fifteen resolve.

The three closures are each smaller and more local than the findings they answer, which is what convergence looks like. The NACK paragraph in particular has now been rewritten three rounds running and is finally saying something true and complete about a case the amendment does not even own — that is the paragraph that exists to be honest about a hole A77 neither opens nor widens, and it now is.

What is left is eight sentences, none of them a decision, none of them in a rule, a condition, a message or a test, and none of them a reason to hold implementation. Fold them into the implementing change if that is cheaper than a docs commit; nothing here needs to land first.
